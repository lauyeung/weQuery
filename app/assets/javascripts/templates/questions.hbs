<div class="row">
  <div class="col-md-4">
    {{outlet}}

  </div>

<div class="col-md-8">
  <h1>I'm the Questions Index</h1>
  {{#each}}
  <p>
    {{!-- <button type="submit" class="btn btn-primary btn-xs" {{action 'vote' this target='controllers.vote'}}>+1</button> --}}
    {{render 'votes.new' this}}
    {{state}}
    {{body}}
    {{votes.length}}
  </p>
{{/each}}
</div>
</div>


